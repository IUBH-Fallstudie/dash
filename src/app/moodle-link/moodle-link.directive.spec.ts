import { MoodleLinkDirective } from './moodle-link.directive';

describe('MoodleLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new MoodleLinkDirective();
    expect(directive).toBeTruthy();
  });
});
